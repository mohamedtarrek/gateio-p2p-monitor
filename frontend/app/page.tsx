/**
 * Main Page
 * Renders the MonitorDashboard component as the root page.
 */

import MonitorDashboard from '@/components/MonitorDashboard';

export default function Home() {
  return <MonitorDashboard />;
}
